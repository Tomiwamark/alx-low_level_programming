#include <stdio.h>
/**
 * main - entry point
 *
 * return: 0
 */
int main(void)
{
char b;
for (b = 48; b <= 57; b ==)
{
putchar(b);
}
for (b = 97; b <= 102; b++)
{
putchar(b);
}
putchar ('\n');

return (0);
}
